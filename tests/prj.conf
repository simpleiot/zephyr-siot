CONFIG_ZTEST=y
CONFIG_LIB_SIOT=y

# the following was required to get strnlen, etc to work
# CONFIG_MINIMAL_LIBC=y
CONFIG_PICOLIBC=y
CONFIG_PICOLIBC_IO_FLOAT=y

# FIXME: the following only applies to native_sim, but I can't
# figure out how to do that yet
CONFIG_FLASH=y
CONFIG_FLASH_SIMULATOR=y
